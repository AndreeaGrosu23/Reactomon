import React, { Component} from 'react';

export default class TypeList extends Component {
    render() {
        return (
            <div>
                pokemon types...
            </div>
        );
    }
}